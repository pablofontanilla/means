// Entry point for the headless engine tuning dashboard (dev-only page).
// M1 scaffold placeholder; tuning UI lands in M2.
const el = document.querySelector<HTMLDivElement>("#sandbox")!;
el.textContent = "Means sandbox — scaffold OK";
